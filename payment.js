// frontend never takes the private keys they hosted over the payment gateway page , tokenzation , or the merchant api keys 

const options = {
  key: "PG_PUBLIC_KEY",
  amount: 50000,
  currency: "INR",
  order_id: "pg_order_123"
}